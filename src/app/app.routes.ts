import { Routes } from '@angular/router';

// Empty routes array is fine - we're using query params on the root path
// The AppComponent will handle query parameter loading via ActivatedRoute
export const routes: Routes = [];
