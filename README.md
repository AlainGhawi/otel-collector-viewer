# OTel Collector Viewer

A visual, interactive editor for OpenTelemetry Collector configuration files — plus an **open-source OTLP JSON log viewer** for files generated by the [OTLP file exporter](https://github.com/open-telemetry/opentelemetry-collector-contrib/tree/main/exporter/fileexporter).


![License](https://img.shields.io/github/license/AlainGhawi/otel-collector-viewer)
![CI](https://img.shields.io/github/actions/workflow/status/AlainGhawi/otel-collector-viewer/ci.yml?label=CI)
![GitHub Pages](https://img.shields.io/github/actions/workflow/status/AlainGhawi/otel-collector-viewer/deploy.yml?label=Deploy)

## Demo

> 🚧 Hosted on [GitHub Pages](https://alainghawi.github.io/otel-collector-viewer/)

Parse, validate, visualize, and edit your OTel Collector pipelines as node-based graphs — then export back to valid YAML. 

![OTel Collector Viewer](docs/config-viewer.png)

Drop in your OTLP file exporter JSON logs and explore them with rich filtering, a severity timeline, and full detail inspection.

![OTel Collector Viewer](docs/log-viewer.png)


## Features

### Config Viewer & Editor

- **YAML ↔ Viewer round-trip**: Import an OTel Collector config and see it as an interactive graph. Edit the YAML and watch the graph update in real time.
- **Layered pipeline visualization**: Receivers, processors, exporters, extensions, and connectors rendered as a left-to-right using d3.js, with color-coded nodes and signal-colored edges.
- **Click-to-inspect**: Click any node to see its full configuration and jump directly to its definition in the YAML editor.
- **Delete components**: Remove nodes from the graph — automatically cleans all pipeline references.
- **Syntax-highlighted YAML editor**: CodeMirror 6 with oneDark theme, line numbers, Tab/Shift+Tab indentation, and undo/redo.
- **Validation & auto-repair**: Detects dangling references, unused components, and empty pipelines. Auto-removes invalid pipeline references. Clickable error links jump to the problem line.
- **Import/Export**: Load YAML files, export edited configs, copy to clipboard.
- **Shareable config links**: Share configurations via URL — click the Share button to copy a link that encodes your entire config. No file uploads required, completely client-side.

### OTLP JSON Log Viewer

No open-source tool exists for viewing the JSON files produced by the OTel Collector's [file exporter](https://github.com/open-telemetry/opentelemetry-collector-contrib/tree/main/exporter/fileexporter) — until now.

- **Drag-and-drop file loading**: Drop one or more `.json` log files and start exploring instantly. Supports both JSONL and pretty-printed JSON formats, including concatenated objects from the file exporter.
- **Web Worker parsing**: Files are parsed off the main thread with progressive chunk loading, so the UI stays responsive even with large files.
- **Severity timeline**: d3.js stacked bar chart shows log distribution over time, color-coded by severity. Brush to filter by time range.
- **Rich filtering**: Filter by severity, service, HTTP status/method/path, time range, and full-text search across all fields.
- **Virtual-scrolled table**: Handles thousands of records with resizable, toggleable columns, sortable timestamps, and per-column filters.
- **Detail inspection**: Expand any row to see the full raw body, parsed JSON fields, resource attributes, log attributes, and body attributes. Copy any record to clipboard.
- **Field extraction**: Automatically extracts HTTP info, user IDs, trace/span IDs, and custom fields from nested JSON body content.
- **Multi-file aggregation**: Load multiple files and view all records together with automatic deduplication.

## Getting Started

### Prerequisites

- Node.js >= 20
- Angular CLI >= 21

### Installation
```bash
git clone https://github.com/AlainGhawi/otel-collector-viewer.git
cd otel-collector-viewer
npm install
```

### Development
```bash
ng serve
```

Navigate to `http://localhost:4200/`. The app reloads automatically on source changes.

### Build
```bash
ng build
```

Build artifacts are stored in the `dist/` directory.

### Tests
```bash
ng test          # Unit tests
ng lint          # Linting
```

## Architecture
```
src/
├── app/
│   ├── core/
│   │   ├── models/          # OTel config data models, validation & error types
│   │   └── services/        # YAML parser, serializer, validator, state management
│   ├── features/
│   │   ├── graph-viewer/    # d3.js layered DAG visualization
│   │   ├── log-viewer/      # OTLP JSON log viewer with timeline, filters & virtual table
│   │   ├── node-detail/     # Click-to-inspect component detail panel
│   │   ├── yaml-panel/      # CodeMirror 6 YAML editor with syntax highlighting
│   │   └── toolbar/         # Import/export/actions toolbar
│   └── shared/              # Reusable components & utilities
public/
    └── sample-configs/      # Example OTel Collector YAML files
```

### Data Flow
```
YAML File → ConfigParserService → OtelConfig Model → GraphViewerComponent (d3.js)
                                       ↕                        ↕
                              ConfigValidatorService    Click/Delete actions
                                       ↕                        ↕
                            ConfigSerializerService ← ConfigStateService (signals)
                                       ↓
                                  YAML Output → CodeMirror Editor
```

## Tech Stack

| Layer            | Technology                |
|:-----------------|:--------------------------|
| Framework        | Angular 21 (zoneless)     |
| Visualization    | d3.js                     |
| Code Editor      | CodeMirror 6              |
| Log Parsing      | Web Workers               |
| YAML Parsing     | js-yaml                   |
| YAML Formatting  | yaml (comment-preserving) |
| State Management | Angular Signals           |
| Styling          | SCSS                      |
| CI/CD            | GitHub Actions            |
| Hosting          | GitHub Pages              |

## Contributing

Contributions are welcome! Please read our [Contributing Guide](CONTRIBUTING.md) and [Code of Conduct](CODE_OF_CONDUCT.md) before submitting a PR.

## Roadmap

- [x] Core YAML parser for OTel Collector configs
- [x] d3.js layered pipeline graph rendering
- [x] Drag-and-drop node repositioning
- [x] Live YAML ↔ graph sync
- [x] Click-to-inspect node details with YAML line linking
- [x] Config validation with auto-repair and clickable error links
- [x] YAML reformatting with comment preservation
- [x] Delete components (graph + pipeline cleanup)
- [x] Export to clipboard / download
- [x] Add new components from a palette/menu
- [x] Component library (known receivers, processors, exporters)
- [x] Dark/light theme toggle
- [x] Shareable config links (URL-encoded)
- [x] OTLP JSON log viewer for file exporter output
- [x] Severity timeline visualization with time-range brushing
- [x] Multi-file log aggregation with deduplication

## License

This project is licensed under the MIT License — see the [LICENSE](LICENSE) file for details.

## Acknowledgments

- [OpenTelemetry](https://opentelemetry.io/) for the incredible observability ecosystem
- [d3.js](https://d3js.org/) for powerful data-driven visualizations
- [CodeMirror](https://codemirror.net/) for the excellent code editor